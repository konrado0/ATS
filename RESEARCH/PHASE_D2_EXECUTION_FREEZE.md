# Phase D2 pre-result execution freeze

Status: **FROZEN BEFORE REAL MODEL FIT**

Phase D2 answers one decision: whether the frozen compact rich-state representation provides material, stable, and economically relevant incremental information beyond both fixed conventional models, especially in the selective long-only swing-opportunity tail.

The cheapest credible experiment is the single frozen eight-block walk-forward run already bound by D0 v3 and D1 v3. No alternative feature, model, label, window, threshold, filter, or subgroup is authorized.

## Gating validity work

- exact pinned input and parent-hash validation;
- PIT membership, denominator 60, preceding-session feature timing, and exact endpoint availability;
- common row populations, fold-local preprocessing, and actual fit/score/evaluation minimums;
- immutable Stage 1 predictions before any evaluation metric;
- sealed Stage 2A, 2B, and 2C outcome exposure in that order;
- identity-neutral fractional score ties and top-contributor boundaries;
- physical and logical validation, independent decisive-metric evaluation, and a complete second logical reproduction.

## Nongating diagnostics

The unselected rich cell, LightGBM C+P+X ablation, frozen proximity-Q5 view, 5/10-session labels, and any small descriptive feature-importance output are diagnostic only. They cannot select or rescue the decisive challenger. MFE, MAE, time-to-excursion, and path-shape outputs are `DEFERRED BY CONTRACT` because no frozen formula exists; D2 will not invent one after results.

## Deferred work

No platform expansion, portfolio simulation, costs, fills, sizing, optimization, D3 execution, or deployment is part of D2.

## Stop/continue rule

- Any material validity, leakage, comparability, availability, integrity, or reproducibility failure: `NOT PROVEN`.
- Valid evidence with any failed frozen research gate: `STOP`.
- Every required frozen gate passing against both conventional comparators wherever required: `CONTINUE`.

Regardless of verdict, D3 and portfolio/backtest execution remain unauthorized pending owner review.

Operational identities and immutable output locations are frozen in `source/python/configs/phase_d2_execution.json`; all scientific choices are derived from `ats_ml.contracts_v3.load_frozen_d0_v3_contract()`.

